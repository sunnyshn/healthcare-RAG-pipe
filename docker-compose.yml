services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      # Persist the corpus and index across container restarts.
      # Matches the container WORKDIR ($HOME/app) used for HF Spaces compatibility.
      - ./data:/home/user/app/data
